name: Content Docker

on:
  push:
    branches: [main]
    paths:
      - "Backend/Shared/Libraries/**.cs"
      - "Backend/Microservices/Content/**.cs"
      - "Backend/Microservices/Content/**.csproj"
      - "Backend/Microservices/Content/Content.Api/Dockerfile"
      - "Backend/Microservices/Content/Content.Api/appsettings.json"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2.5.0

      - name: Docker Login
        uses: docker/login-action@v2.1.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build image and push to Docker Hub
        uses: docker/build-push-action@v3.2.0
        with:
          context: ./Backend
          file: ./Backend/Microservices/Content/Content.Api/Dockerfile
          tags: ${{ secrets.DOCKER_USERNAME }}/sc-api-content:latest
          push: true
